2026-03-05 - Completed Phase 1 (Reference Rewriter) as highest-priority dependency for plan sync.
- Added `src/reference_rewriter.rs` with `rewrite_issue_urls(markdown, local_ids)` for GitHub issues/PRs and Linear issue URLs.
- Rewrites only when local file ID exists; skips markdown link destinations to avoid double-wrapping; leaves fenced code blocks unchanged.
- Added 8 unit tests covering all Phase 1 acceptance criteria plus fenced code-block safety.
- Feedback loops run: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo nextest run -E 'test(reference_rewriter)'` (all pass).

2026-03-05 - Implemented Phase 2 trait groundwork as highest-priority unblocker for plan sync.
- Added `fetch_sub_issues(task_id) -> Result<Vec<Task>>` to `TaskSource` and wired dispatch in `AnySource`.
- Implemented `GitHubSource::fetch_sub_issues` via `gh issue view --json subIssues`, supporting both list and `{ nodes: [...] }` payload shapes.
- Added `LinearSource::fetch_sub_issues` stub returning empty vec (compile-safe placeholder per plan).
- Added/updated tests: 2 new GitHub sub-issue unit tests, plus TaskSource test mocks updated for new trait method.
- Feedback loops run: `cargo fmt --all -- --check`, `cargo check`, `cargo nextest run -E 'test(test_fetch_sub_issues_parses_connection_shape) or test(test_fetch_sub_issues_handles_missing_field)'` (all pass).

2026-03-05 - Completed Phase 2 plan-sync module as highest-priority dependency for local/sync-first orchestration.
- Added `src/plan_sync.rs` with `sync_to_local(source, issue_id, plans_dir) -> PlanDirectory` and `list_plan_files(plan_dir)`.
- Sync flow now fetches main issue, fetches + hydrates sub-issues, crawls referenced issues up to depth 4 with deduplication, writes `<id>.md` files, then rewrites cross-links via `reference_rewriter`.
- Reused `WorktreeManager::slugify` for plan folder naming with fallback `gh-<issue_id>` when title slug is empty.
- Added 4 unit tests covering directory structure, sub-issue inclusion, depth limiting, circular deduplication, link rewriting, and slug fallback.
- Feedback loops run: `cargo fmt --all -- --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo nextest run -E 'test(plan_sync)'` (all pass).

2026-03-05 - Completed highest-priority Phase 3 CLI groundwork for local plan source (`plan_path` parsing + validation).
- Added optional positional `plan_path` to `rlph build` (`BuildArgs`) and plumbed it into `Config` as `plan_path: Option<String>`.
- Added build-time local plan directory validation in `main`: path must exist, be a directory, and include at least one file.
- Updated config/test helpers to include new `Config.plan_path` field (`src/prd.rs`, `tests/common/mod.rs`, `tests/prd_integration.rs`) to keep type checks green.
- Added tests for CLI parsing, config merge, and local plan directory validation success/failure cases.
- Feedback loops run: `cargo fmt --all -- --check`, `cargo check`, `cargo nextest run -E 'test(test_parse_build_with_plan_path) or test(test_plan_path_from_build_args) or test(validate_local_plan_dir_accepts_directory_with_files) or test(validate_local_plan_dir_rejects_missing_directory) or test(validate_local_plan_dir_rejects_empty_directory)'` (all pass).

2026-03-05 - Completed highest-priority remaining Phase 3 task: local mode now bypasses `TaskSource` fetch loop.
- Updated `Orchestrator::run_iteration` to short-circuit into a local-plan iteration when `Config.plan_path` is set; no `fetch_eligible_tasks`, `fetch_closed_task_ids`, or `get_task_details` calls in this path.
- Added local-plan task bootstrap with task identity derived from the plan directory slug and reused implement/review flow without source status marking.
- Updated PR body generation for local plans to reference the plan path instead of adding `Resolves #...`.
- Added integration coverage `test_local_plan_mode_skips_task_source_fetch` using a fail-fast `TaskSource` implementation.
- Feedback loops run: `cargo fmt --all -- --check`, `cargo check`, `cargo nextest run -E 'test(test_local_plan_mode_skips_task_source_fetch)'` (all pass).
