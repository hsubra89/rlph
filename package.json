{
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint --parallel"
  },
  "devDependencies": {
    "oxfmt": "^0.37.0",
    "oxlint": "^1.52.0"
  }
}